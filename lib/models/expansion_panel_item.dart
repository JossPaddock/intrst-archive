class Item {
  String header;
  String body;
  // String itemId;
  // bool isExpanded;

  Item({
    required this.header,
    required this.body,
    // required this.itemId,
    // this.isExpanded = false,
  });
}